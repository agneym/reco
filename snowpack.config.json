{
  "scripts": {
    "mount:public": "mount public --to .",
    "mount:web_modules": "mount web_modules",
    "mount:src": "mount src --to _dist_",
    "build:js": "babel --filename $FILE --ignore public/ffmeg-worker-mp4.js",
    "build:css": "postcss"
  },
  "knownEntrypoints": [
    "htm",
    "preact",
    "preact/hooks",
    "htm/preact",
    "classnames",
    "video-stream-merger",
    "framer-motion",
    "enumerate-devices",
    "rc-slider",
    "get-blob-duration",
    "file-saver"
  ],
  "installOptions": {
    "alias": {
      "react": "preact/compat", 
      "react-dom": "preact/compat"
    }
  }
}